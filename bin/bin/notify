#!/usr/bin/env bash
output=""

#while [ $# ]; do
    #if the command exists run it
    if command -v "$1" > /dev/null; then
        output="$output$($1)"
    else
        output="$output$1"
    fi
#    shift
#done
notify-send "$output"
